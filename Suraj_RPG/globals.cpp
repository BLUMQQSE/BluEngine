#include "pch.h"
#include "globals.h"

namespace bm98
{
const sf::Color Color::Teal = sf::Color(23, 218, 205, 255);
const sf::Color Color::Orange = sf::Color(250, 160, 24, 255);
const sf::Color Color::LimeGreen = sf::Color(128, 255, 0, 255);
const sf::Color Color::LightGrey = sf::Color(200, 200, 200, 255);
const sf::Color Color::Grey = sf::Color(150, 150, 150, 255);
const sf::Color Color::Charcoal = sf::Color(90, 90, 90, 255);
}